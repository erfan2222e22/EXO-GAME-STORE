import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";
const Production_items = styled(Box)({
  width: "300px",
  height: "100px",
  margin: "10px",
  position: "relative",
  left: "25px",
});
export default {
  Production_items,
};
