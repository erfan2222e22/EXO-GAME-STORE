import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

const EmptyShopingCart = styled(Box)({
  width: "390px",
  boxShadow: " rgba(99, 99, 99, 0.2) 0px 2px 8px 0px",
  position: "absolute",
  height: "200px",
  // zIndex: "1000",
});

const styleComponent = {
  EmptyShopingCart,
};

export default styleComponent;
