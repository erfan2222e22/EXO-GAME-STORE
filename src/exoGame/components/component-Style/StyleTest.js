import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

const PrentBox = styled(Box)({
  display: "flex",
  gap: "3%",
  borderBottom: "2px solid black",
});

const PrentBox1 = styled(Box)({
  display: "flex",
  gap: "3%",
  borderBottom: "2px solid black",
});

const styleComponent = {
  PrentBox1,
  PrentBox,
};
export default styleComponent;
